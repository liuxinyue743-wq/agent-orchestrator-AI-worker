# 操作说明 · CL-AO 闭环智能体控制台

> 面向最终用户的上手手册。开发者集成见 `README.md` 与 `ARCHITECTURE-v0.2.md`。

## 1. 前置条件

| 依赖 | 说明 |
|---|---|
| Windows 10/11 x64 | 已验证平台 |
| Python 3.10+（加入 PATH） | `安装.bat` 据此建虚拟环境 |
| Agent Orchestrator（AO）v0.12.9 | 运行 `scripts\Install-AO.ps1` 下载官方安装器，或见安装文档 |
| Claude CLI + Git Bash | Planner/Auditor/Verifier 经 headless `claude` 调用；Git Bash 是 CLI 在 Windows 的硬性依赖 |
| 网络连接 | 调用模型 API（按所选提供方产生费用） |

## 2. 首次安装（一次性）

1. **安装 AO v0.12.9**：运行 `powershell -ExecutionPolicy Bypass -File .\scripts\Install-AO.ps1 -RunInstaller`。
2. **初始化示例仓库**：运行 `powershell -ExecutionPolicy Bypass -File .\scripts\Initialize-Demo.ps1`，再在 AO 中注册该项目。
2. 双击 **`安装.bat`**：自动建虚拟环境 → 装依赖 → 跑离线自检（约 1–3 分钟，看到 "安装完成" 即可）。

> 自检失败表示当前环境尚未达到发布基线，`安装.bat` 会停止并返回错误；应先修复 Python 或依赖问题。

## 3. 日常启动（三步）

1. **启动 AO 桌面应用** —— 守护进程监听 `127.0.0.1:3001`。
2. 双击 **`启动面板.bat`** —— 浏览器自动打开 `http://127.0.0.1:7100`。
3. 在面板里点 **「＋ 新建任务」** 填表 → **「▶ 启动闭环」**。

## 4. 发起一个任务（大白话，无需懂代码）

在「新建 Mission」表单中：

| 栏目 | 含义 | 示例 |
|---|---|---|
| 任务目标 * | 要做什么（自然语言） | `在 app.py 新增 power(a,n) 返回 a 的 n 次方，配 pytest` |
| 验收条件 * | 每行一条可判对错的硬条件 | `power(2,3)==8` |
| 允许路径 | Worker 只准改这些（相对仓库根） | `app.py`、`tests/**` |
| 门禁命令 | 验收时 Gate 执行，返回码 0 即过 | `python -m pytest -q` |
| 最大子任务数 | 并行 Worker 上限，小任务填 1 | `1` |

点「启动闭环」后全程无需操作：拓扑图、事件流、状态迁移实时可见；
`memory.md` / `project.md` 自动记录项目记忆与重大事项；最终结果由 Planner 汇总报告。

结束状态：`MISSION_DONE`（验收全过）/ `HUMAN`（受控转人工）。

## 5. 任务运行中能做什么

- **停止**：点「■ 停止」→ 状态落 HUMAN，可随时「续跑」。
- **下达指令**：底部「✉ 指令」栏，选目标 Agent（Planner / Auditor / Verifier / Observer / Gate / 任一 Worker）。
  - 发给 Planner 的指令仅 Planner 可见；
  - 发给其他 Agent 的指令**自动镜像一份给 Planner**（用户的一切指令 Planner 必然可见）。
- **实时调参**：「时间参数」面板改轮询/冷却/升级阈值，点「应用」即时生效（改完务必点应用，否则会被下一帧覆盖）。
- **自动合并 master**：勾选「DONE 后自动合并 master（含推 origin）」后，任务通过验收会把目标仓库 master 快进到已验证集成头并推送 origin（AO 的 worker 基线取自 origin/master，必须推）。

## 6. 模型中心（可选）

「模型中心」支持登记多个模型提供方（claude-cli / kimi-cli / codex-cli），并给每个角色单独下拉选择，
保存后对**下一个新任务**生效。配置存于 `config/providers.json`。

- 默认使用本机 Claude CLI 的默认模型；GLM、Kimi、Codex 需在本机对应 CLI/网关中完成配置。
- Worker 由 AO 孵化，模型名需 AO 网关可用；codex 提供方仅建议用于只读角色。

## 7. 存储后端（可选）

「存储后端」卡片：默认 SQLite（零配置，每任务一个 `runtime/<mission-id>/state.db`）；
可选 PostgreSQL（每任务一个 schema，需自行提供实例）。切换对下一个新任务生效，历史存档双后端都可查看/续跑。

> PostgreSQL DSN 含密码，会以明文存于 `config/storage.json`（已在 `.gitignore`，不会入库）；
> 面板回显用掩码。生产环境建议改用环境变量。

## 8. 历史任务

左上「历史任务」列表：点「续跑」从断点恢复，点「查看」只读挂载（不跑、不调模型）。

## 9. 常见问题

| 现象 | 处理 |
|---|---|
| 面板打不开 / 端口占用 | 设环境变量 `PANEL_PORT=7101` 后再跑 `启动面板.bat` |
| 任务停在 `HUMAN` | 正常——预算超限或冲突受控转人工；看「事件流」与 `memory.md`/`project.md` 定位 |
| Worker 一直不工作 | 先确认 AO 桌面应用已启动、`closed-loop-demo` 项目已注册、模型网关可用 |
| 自检失败 | 多半是 Python 版本/依赖问题；手动 `.venv\Scripts\python -m pip install -r requirements.txt` |

## 10. 诚实边界

- 本系统是**本地集成技术原型 / 比赛演示控制台**，不是零配置商业产品；首次使用需完成"首次安装"。
- 模型调用需联网并产生 API 费用。
- "完成"以 Gate 真实测试和配置启用时的独立 Verifier 复核为准；Worker 自报完成不作数。
